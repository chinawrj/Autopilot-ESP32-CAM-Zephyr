# Day 003 — HTTP Server + MJPEG Test Stream

**Date**: 2026-04-19
**Milestone**: M1 — Basic TCP Video Stream
**Status**: ✅ Complete

## Goals

1. [x] HTTP server on port 80 (socket-based, Zephyr BSD sockets)
2. [x] MJPEG streaming endpoint `/stream/tcp` with embedded test JPEG pattern
3. [x] Browser-verified: page loads + MJPEG stream displays in browser

## Architecture Decisions

- **Camera driver deferred**: ESP32 (original) has no Zephyr DVP driver; I2S camera mode
  requires custom driver work. Building HTTP/MJPEG infrastructure first with test pattern.
- **Connection model**: 1 listener thread + 1 streaming worker, max 1 concurrent stream client.
  Send timeouts for slow-client protection.
- **Frame source abstraction**: `frame_source_get()` returns current JPEG frame; camera
  backend plugs in later without HTTP layer changes.
- **Test JPEG**: Embedded 160x120 color-bar JPEG in flash/rodata (~2-4 KB).
- **Socket API**: `CONFIG_POSIX_API=y` for POSIX socket names (Zephyr 4.1 removed POSIX_NAMES).

## What Was Done

### Task 1: HTTP Server ✅
- Created `src/http_server.c/h` — BSD socket HTTP server on port 80
- Listener thread + sequential client handling
- `GET /` → embedded HTML page with cyan-themed UI, embedded `<img>` for MJPEG stream
- `GET /stream/tcp` → MJPEG multipart/x-mixed-replace stream
- Send timeout (5s) to protect against slow/stalled clients
- Zero warnings

### Task 2: MJPEG Test Stream ✅
- Created `src/frame_source.c/h` — abstraction for frame sources
- Embedded 160x120 color-bar JPEG test pattern (1851 bytes, generated with Pillow)
- `frame_source_get()` returns current JPEG; camera backend plugs in later
- MJPEG streaming at ~5 FPS (200ms delay) with proper boundary format
- Proper `Cache-Control`/`Pragma` no-cache headers

### Task 3: Browser Verification ✅
- curl: `GET /` returns HTTP 200, 688 bytes HTML
- curl: `GET /stream/tcp` returns proper MJPEG multipart with JPEG frames
- Chrome browser: page loads, color-bar test pattern streams live in `<img>` tag
- Serial logs confirm: all requests logged, stream start/stop clean

## Build Issues Resolved
1. Missing `#include <stdio.h>` for `snprintf` in Zephyr minimal libc
2. WiFi credentials not injected — must use `-DOVERLAY_CONFIG=wifi-credentials.conf`

## Memory Report
- DRAM: ~94.77% used (tight — may need optimization)
- PSRAM: ~75.80% used
- Flash firmware: ~527 KB

## Architecture Notes
- ESP32 (original) has NO Zephyr video/DVP driver — only ESP32-S3 has LCD_CAM support
- Camera integration will require custom I2S camera mode driver or esp32-camera port
- HTTP/MJPEG infrastructure is ready; swap test pattern for real camera frames later
